<mule xmlns="http://www.mulesoft.org/schema/mule/core"
      xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
      xmlns:akv="http://www.mulesoft.org/schema/mule/akv"
      xsi:schemaLocation="http://www.mulesoft.org/schema/mule/core http://www.mulesoft.org/schema/mule/core/current/mule.xsd
          http://www.mulesoft.org/schema/mule/akv http://www.mulesoft.org/schema/mule/akv/current/mule-akv.xsd">

    <akv:config name="config" >
        <akv:connection tenantId="MOCK_TENANT" clientId="MOCK_CLIENT_ID" clientSecret="MOCK_SECRET" baseUri="${akvUrl}" />
    </akv:config>

    <flow name="getSecretSuccess">
        <akv:get-secret config-ref="config" path="${akvUrl}/secrets/mysecret"/>
    </flow>

    <flow name="getSecretNotFound">
        <akv:get-secret config-ref="config" path="${akvUrl}/secrets/mysecretnotfound"/>
    </flow>

    <flow name="getKeySuccess" >
        <akv:get-key config-ref="config" path="${akvUrl}/keys/test-poc-key-avio"/>
    </flow>

    <flow name="getKeyNotFound">
        <akv:get-secret config-ref="config" path="${akvUrl}/keys/test-poc-keynotfound"/>
    </flow>

    <flow name="getCertificateSuccess">
        <akv:get-certificate config-ref="config" path="${akvUrl}/certificates/mockcertificate"/>
    </flow>

    <flow name="getCertificateNotFound">
        <akv:get-certificate config-ref="config" path="${akvUrl}/certificates/mockcertificatenotfound"/>
    </flow>
</mule>
